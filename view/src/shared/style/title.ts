import { type CSSResultGroup, css } from "lit";

export const titleStyle: CSSResultGroup = css`
  .section-title {
    margin: 0 0 8px 0;
    font-size: 14px;
    color: var(--color-fg-muted);
  }
`;
